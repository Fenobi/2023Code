#define _CRT_SECURE_NO_WARNINGS

#include <iostream>
#include <string>
#include <string.h>

using namespace std;

int main()
{
    string s;
    cin >> s;
    string::iterator it = s.begin();
    for (int i = 0; i < s.size(); ++i)
    {
        strstr()
    }
    return 0;
}